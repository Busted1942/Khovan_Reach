# CLAUDE.md — Khovan Reach

**`AGENTS.md` in this directory is the single governing control file for this repo — read it before any artifact-changing work.**

This file exists only because Claude Code auto-loads a file named `CLAUDE.md`. It carries no rules of its own; every rule that used to live here has been merged into `AGENTS.md` so there is one source of truth instead of two files that can drift out of sync. If you are reading this and have not yet read `AGENTS.md`, stop and read it first — it covers source authority, work boundaries, orientation and repo shape, MAST-writing rules, evidence classes, Git/branch discipline, and the operator test-expectation format.
